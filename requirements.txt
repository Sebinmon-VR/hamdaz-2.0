fastapi>=0.115
uvicorn[standard]>=0.32
pydantic[email]>=2.9
pydantic-settings>=2.6
# Arrives with pydantic-settings anyway, but run.py imports it directly to let
# .env beat a stale shell variable in development — so it is declared here.
python-dotenv>=1.0
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14
psycopg[binary,pool]>=3.2
pyjwt[crypto]>=2.10
httpx>=0.28
anthropic>=0.69
openai>=3.8
python-multipart>=0.0.20
openpyxl>=3.1
python-docx>=1.1
pdfplumber>=0.11
# Writes the PDF a report is exported as. Named here rather than left as
# something pdfplumber happens to pull in: a transitive dependency that
# disappears takes a feature with it, and pure Python is why this is the
# PDF writer rather than one with native libraries to install.
reportlab>=4.0
# Optional: reads scanned and photographed documents (supplier quotes, RFQs)
# through local OCR — no model, no network, about 10–40 s a page on a CPU.
# Uncomment to install; without it a scan is declined with "type it in".
# rapidocr-onnxruntime>=1.3
