[pytest]
testpaths = tests
asyncio_mode = auto
# Every test coroutine shares one loop, so the engine created in a session-scoped
# fixture is still valid inside each test.
asyncio_default_fixture_loop_scope = session
filterwarnings =
    ignore::DeprecationWarning
