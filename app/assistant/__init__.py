"""The assistant: a chat agent over the ERP's own modules, run as the person asking.

See ``catalogue.py`` for what it can do, ``policy.py`` for who gets what, and
``agent.py`` for the loop that ties them to OpenAI.
"""
